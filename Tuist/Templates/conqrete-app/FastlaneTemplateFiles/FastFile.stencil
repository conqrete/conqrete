default_platform(:ios)

platform :ios do

  desc "Build & Test"
  lane :test do
    run_tests(
      workspace: "{{ name }}.xcworkspace",
      scheme: "{{ name }}",
      code_coverage: true
    )
  end

end
