default_platform(:ios)

platform :ios do

  desc "Build & Test"
  lane :test do
    run_tests(
      workspace: "{{ projectName }}.xcworkspace",
      scheme: "{{ projectName }}",
      code_coverage: true
    )
  end

end
